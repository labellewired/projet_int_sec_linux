To do:
-Read + Notes X
-Schema (à mod à la fin)
-Organise Github
-kickstarts
-Ansible
-matrice de flux
-Rapport d'audit

À remettre:
-Link Github
-Schema
-matrice de flux
-Rapport d'audit
-Fichiers de configuration 
--fichier de durcissement de SSH
--configuration Fail2ban (jail ainsi que les autres fichiers modifiés, s’il y a lieu)
--fichier de site Nginx
--fichier de zone DNS
--fichier `chrony.conf`
--configuration du collecteur `rsyslog`
--règles `auditd`
--l’export XML de vos zones firewalld personnalisées (s’il y a lieu)

Parler un playbook pour les 3 ou 3 playbooks?

Architecture:
-public			10.10.1.0/24
--bastion.acme.lan	10.10.1.10
--web.acme.lan		10.10.1.20
--infra.acme.lan	10.10.1.40
--Poste client		10.10.1.200
-admin			10.10.10.0/24
--bastion.acme.lan	10.10.10.10
--web.acme.lan		10.10.10.20
--infra.acme.lan	10.10.10.40
--Poste client		10.10.10.200

bastion.acme.lan:
-ssh (unique externe)
-contrôleur ansible (target itself)
----
-comptes: alabelle + zlizotte
-SELinux en enforcing
-Zone par défaut: drop
-Chaque carte réseau affectée explicitement à une zone firewalld
-Journal systemd survit au redémarrage
-Heure synchronisée sur infra
-Aucune connexion root en SSH autorisée
-sudo journalisé
-Comptes et services inutiles retirés
----
-Chronyd target infra
-Depuis le réseau public, connexion SSH n'accepte que l'authentification par clé ed25519, (pas MdP)
-Le code TOTP est exigé en plus de la clé
-Fail2ban bannit après cinq échecs consécutifs
-Les tentatives SSH sont journalisées par une règle enrichie (firewalld rich rule) avec préfixe et limite de débit


web.acme.lan:
-Nginx en HTTPS (application et sa base de données)
----
-comptes: alabelle + zlizotte
-SELinux en enforcing
-Zone par défaut: drop
-Chaque carte réseau affectée explicitement à une zone firewalld
-Journal systemd survit au redémarrage
-Heure synchronisée sur infra
-Aucune connexion root en SSH autorisée
-sudo journalisé
-Comptes et services inutiles retirés
----
-Chronyd target infra
-https://www.acme.lan est servi sur le segment public
-le port 80 redirige vers le port 443
-seuls TLS 1.2 et 1.3 sont acceptés
-/admin est refusé depuis le poste client et accepté depuis le poste d'administration après authentification
-MariaDB n'écoute que sur 127.0.0.1, le port 3306 n'est ouvert dans aucune zone
-SSH n'est joignable que depuis le segment admin et le rebond depuis le bastion fonctionne

infra.acme.lan:
-DNSz
-temps
-collecteur de journaux
-outils d'audit
----
-comptes: alabelle + zlizotte
-SELinux en enforcing
-Zone par défaut: drop
-Chaque carte réseau affectée explicitement à une zone firewalld
-Journal systemd survit au redémarrage
-Heure synchronisée sur infra
-Aucune connexion root en SSH autorisée
-sudo journalisé
-Comptes et services inutiles retirés
----
-Chronyd
-DNS fait autorité sur acme.lan et répond aux clients du segment public pour les 3 serveurs
-Le collecteur rsyslog reçoit les journaux de bastion et web et refuse toute autre source
-SSH n'est joignable que depuis le segment admin et le rebond depuis le bastion fonctionne

